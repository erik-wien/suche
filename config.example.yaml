# Config template — copy to config.yaml and fill in your values

target: akadbrain
db:
  host: localhost
  socket: /tmp/mysql.sock
  name: auth
  user: suche
  password: your_password
deploy:
  ssh_user: erik
  ssh_host: akadbrain.local
  ssh_key: ~/.ssh/id_rsa
  dest: /Library/WebServer/Documents/suche
  sync_dirs:
  - web
  - inc
  - vendor
auth_db:
  host: localhost
  socket: /tmp/mysql.sock
  name: auth
  user: suche
  password: your_password
slack:
  bot_token: your_bot_token
app:
  name: Suche
  support_email: contact@eriks.cloud
  color: '#ffffff'
  base_url: https://suche.eriks.cloud
APP_CODE: suche
