# Config template — copy to config.yaml and fill in your values

target: local
db:
  host: localhost
  socket: /tmp/mysql.sock
  name: jardyx
  user: suche
  password: your_password
deploy:
  web_root: /Library/WebServer/Documents
  dest: /Library/WebServer/Documents/suche
  sync_dirs:
  - web
  - inc
  - vendor
auth_db:
  host: localhost
  socket: /tmp/mysql.sock
  name: jardyx
  user: suche
  password: your_password
slack:
  bot_token: your_bot_token
app:
  name: Suche
  support_email: contact@eriks.cloud
  color: '#ffffff'
  base_url: http://suche.test
  env: local
APP_CODE: suche
